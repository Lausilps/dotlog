O Dot em pessoa — imagem pixelada com flutuação suave. Passe o caminho do asset.

```jsx
<Mascote src="assets/mascote-dot.png" size={160} />
```

Assets: `mascote-dot.png` (pose padrão), `emotes-dot.png` (folha de 22 estados — recorte antes de usar).
